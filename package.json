{
  "name": "game-watch",
  "version": "1.0.0",
  "author": "Fedor Alekseev",
  "scripts": {
    "init": "npm run init:proxy && npm run init:client",
    "init:proxy": "cd proxy && npm ci",
    "build:proxy": "cd proxy && npm run build",
    "start:proxy": "cd proxy && npm start",
    "dev:proxy": "cd proxy && npm run dev",
    "init:client": "cd client && npm ci",
    "build:client": "cd client && npm run build",
    "dev:client": "cd client && npm start"
  }
}
